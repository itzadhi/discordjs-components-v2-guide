# Discord.js Production Example Bot

This example uses discord.js v14+, slash commands, modern component routing, service boundaries, and environment variables.

Run:

```bash
npm install
npm run typecheck
```
